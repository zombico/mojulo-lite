// Mojulo-Lite ships English-only for now. The i18n lib is preserved so that
// copied components which call useTranslations() continue to work, and so that
// new locales can be added without restructuring.
export const locales = ['en'];
export const defaultLocale = 'en';

export const localeNames = {
  en: 'English',
};
